{{- if .Values.dashboard.create -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lomas.dashboard.fullname" . }}
  labels:
    {{- include "lomas.dashboard.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "lomas.dashboard.selectorLabels" . | nindent 6 }}
  replicas: 1
  template:
    metadata:
      {{- with .Values.dashboard.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "lomas.dashboard.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "lomas.dashboard.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.dashboard.podSecurityContext | nindent 8 }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8}}
      containers:
      - name: {{ .Chart.Name }}
        securityContext:
            {{- toYaml .Values.dashboard.securityContext | nindent 12 }}
        resources:
          {{- toYaml .Values.dashboard.resources | nindent 10 }}
        image: "{{ .Values.dashboard.image.repository }}:{{ .Values.dashboard.image.tag }}"
        imagePullPolicy: {{ .Values.dashboard.image.pullPolicy }}
        command:
          - lomas-dashboard
        ports:
          - name: http
            containerPort: {{ .Values.dashboard.serverConfig.port }}
            protocol: TCP
        # volumeMounts:
        #   - name: data
        #     mountPath: /persistent-storage
        env:
            - name: LOMAS_ADMIN_SERVER_SERVICE
              value: {{ .Values.dashboard.env.adminServerService }}
            - name: LOMAS_ADMIN_SERVER_URL
              value: {{ .Values.dashboard.env.adminServerUrl }}
            - name: STREAMLIT_SECRETS_FILES
              value: {{ .Values.dashboard.env.streamlitSecretsFile }}

        volumeMounts:
          - name: config
            mountPath: /config

      volumes:
        - name: config
          configMap:
            name: {{ include "lomas.dashboard.fullname" . }}-auth

      {{- with .Values.dashboard.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.dashboard.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.dashboard.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
