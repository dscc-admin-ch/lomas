apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "lomas.loki.fullname" . }}
  labels:
    {{- include "lomas.loki.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.loki.pvc.persistence.resourcePolicy }}
    "helm.sh/resource-policy": {{ .Values.loki.pvc.persistence.resourcePolicy }}
    {{- end }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.loki.pvc.storageClassName }}
  storageClassName: {{ .Values.loki.pvc.storageClassName | quote }}
  {{- end }}
  resources:
    limits:
      storage: {{ .Values.loki.pvc.sizeLimit | quote }}
    requests:
      storage: {{ .Values.loki.pvc.sizeRequest | quote }}