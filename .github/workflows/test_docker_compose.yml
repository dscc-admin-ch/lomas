name: Docker Compose Tests

on:
  pull_request:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  tests:
    name: Run tests on Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: cachix/install-nix-action@v31.10.1
    - uses: cachix/cachix-action@v17
      with:
        name: lomas
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'

    - name: Install devenv.sh
      run: nix profile add nixpkgs/d233902339c02a9c334e7e593de68855ad26c4cb#devenv

    - name: Run a notebook within a container
      run: |
        devenv shell
        devenv shell -- docker-load-image
        devenv shell -- docker-compose-test
