{{- if .Values.caddy.create -}}
{{- if .Values.caddy.route.enabled -}}
{{- $fullName := include "lomas.caddy.fullname" . -}}
{{- $svcPort := .Values.caddy.service.portName -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "lomas.caddy.labels" . | nindent 4 }}
  {{- with .Values.caddy.route.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  host: {{ .Values.caddy.hostname | quote }}
  path: /
  port:
    targetPort: {{ $svcPort }}
  to:
    kind: Service
    name: {{ $fullName }}
  tls:
    {{- toYaml .Values.caddy.route.tls | nindent 4 }}
{{- end }}
{{- end }}