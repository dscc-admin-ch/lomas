# General stuff
global:
  imagePullSecrets: [] # list of name: secret.
nameOverride: ""
fullnameOverride: "lomas"

# Server / Worker
##########################################################################
server:
  image:
    repository: dsccadminch/lomas
    imagePullPolicy: Always
    tag: sha-4c90599

  oidc_discovery_url: http://auth-lomaslm.lab.sspcloud.fr/.well-known/openid-configuration
  # Runtime args for server
  runtime_args:
    server:
      host_port: "48080"
      log_level: "info"
      submit_limit: 300
    authenticator:
      authentication_type: jwt
    opendp_features: ["contrib", "floating-point", "honest-but-curious"]
    private_db_credentials:
      # - existing_secret: "" # Set this if there is an existing secret.
      # - credentials_name: "s3" # Or set the credentials directly.
      #  db_type: "S3_DB"
      #  access_key_id: "abc"
      #  secret_access_key: "def"
    telemetry:
      enabled: true
      service_name: lomas_server_app
      service_id: default-host
  pvc:
    storageClassName: "" # is not set if empty string
    sizeLimit: 500Mi
    sizeRequest: 500Mi
    accessMode: "ReadWriteOnce" # ReadWriteMany if supported, otherwise use affinity to colocate server, worker and dashboard.
    persistence:
      # If set to "keep", sets the helm/resource-policy of the pvc to keep,
      # so that the pvc is not deleted across reinstalls.
      resourcePolicy: ""

  service:
    type: ClusterIP
    port: 80
    portName: http

  ingress:
    enabled: true
    className: "nginx"
    annotations:
      nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    hostname: "lomas-lance.lab.sspcloud.fr"
  # Openshift route for Openshift clusters.
  route:
    enabled: false
    annotations: {}
    hostname: "lomas-lance.lab.sspcloud.fr"
    tls:
      insecureEdgeTerminationPolicy: Redirect
      termination: edge

  # We leave unset stuff here
  serviceAccount:
    create: false
    annotations: {}
    name: ""
  resources: {}
    # limits:
    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  podAnnotations: {}
  podSecurityContext: {}
  securityContext: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}

worker:
  replicaCount: 1
  serviceAccount:
    create: false
    annotations: {}
    name: ""
  resources: {}
  podAnnotations: {}
  podSecurityContext: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}


# Lomas Administration
##########################################################################
admin:
  image:
    repository: dsccadminch/lomas
    pullPolicy: Always
    tag: sha-4c90599
  curlImage:
    repository: curlimages/curl
    tag: 8.13.0
    pullPolicy: Always

  realm: "lomas"
  client_id: "lomas_admin"
  client_secret: "lomas_admin"
  client_secret_existing_secret: ""
  client_secret_secret_key: ""
  lomas_service_client_id: "lomas_service"
  lomas_service_client_secret: "lomas_service"
  lomas_service_existing_secret: ""
  lomas_service_secret_key: ""
  initContainers:
    resources:
      limits:
        cpu: 100m
        memory: 400Mi
      requests:
        cpu: 50m
        memory: 400Mi
  demo_setup:
    enable: true
    path_prefix: "/data"
    user_yaml_path: "/collections/user_collection.yaml"
    dataset_yaml_path: "/collections/dataset_collection.yaml"

  # We leave unset stuff here
  serviceAccount:
    create: false
    annotations: {}
    name: ""
  resources: {}
    # limits:    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  podAnnotations: {}
  podSecurityContext: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}

dashboard:
  create: true

  image:
    repository: dsccadminch/lomas
    pullPolicy: Always
    tag: sha-4c90599

  # Runtime args for server
  serverConfig:
    address: "0.0.0.0"
    port: "8501"

  service:
    type: ClusterIP
    port: 80
    portName: http

  ingress:
    enabled: true
    className: "nginx"
    annotations:
      nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    hostname: "lomas-dashboard-lance.lab.sspcloud.fr"
  # Openshift route for Openshift clusters.
  route:
    enabled: false
    annotations: {}
    hostname: "lomas-dashboard-lance.lab.sspcloud.fr"
    tls:
      insecureEdgeTerminationPolicy: Redirect
      termination: edge
  # Lomas dashboard not meant to be replicated for now
  replicaCount: 1
  # Lomas dashboard not intended to be autoscaled for now
  # autoscaling:
  #  enabled: false
  #  minReplicas: 1
  #  maxReplicas: 100
  #  targetCPUUtilizationPercentage: 80
  #  # targetMemoryUtilizationPercentage: 80

  # We leave unset stuff here
  serviceAccount:
    create: false
    annotations: {}
    name: ""
  resources: {}
    # limits:
    #   cpu: 100m
    #   memory: 128Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi
  podAnnotations: {}
  podSecurityContext: {}
    # fsGroup: 2000
  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000
  nodeSelector: {}
  tolerations: []
  affinity: {}

  env:
    adminServerService: "https://lomas-lance.lab.sspcloud.fr"
    adminServerUrl: "https://lomas-lance.lab.sspcloud.fr"
    streamlitSecretsFile: "/config/secrets.toml"

  auth:
    clientId: lomas_dashboard
    clientSecret: lomas_dashboard
    redirectUri: https://lomas-dashboard-lance.lab.sspcloud.fr/admin/oauth2callback
    serverMetadataUrl: https://auth-lomaslm.lab.sspcloud.fr/.well-known/openid-configuration
    cookieSecret: changeme
    exposeTokens:
      - access
      - id

# Loki
##########################################################################
loki:
  create: false

  image:
    repository: grafana/loki
    tag: 3.4

  service:
    port: 3100
    target_port: 3100
    type: ClusterIP

  fullnameOverride: "lomas-loki"

  config:
    auth_enabled: false
    limits_config:
      allow_structured_metadata: true
      volume_enabled: true
    common:
      ring:
        instance_addr: 0.0.0.0
        kvstore:
          store: inmemory
      replication_factor: 1
      path_prefix: /tmp/loki
    schema_config:
      configs:
        - from: "2025-01-10"
          store: tsdb
          object_store: filesystem
          schema: v13
          index:
            prefix: loki_index_
            period: 24h
    storage_config:
      tsdb_shipper:
        active_index_directory: /tmp/loki/index
        cache_location: /tmp/loki/index_cache
      filesystem:
        directory: /tmp/loki/chunks
    pattern_ingester:
      enabled: true

  podSecurityContext: {}
    # fsGroup: 10001 # Match container group rights for volume access
  pvc:
    storageClassName: "" # is not set if empty string
    sizeLimit: 1Gi
    sizeRequest: 1Gi
    persistence:
      # If set to "keep", sets the helm/resource-policy of the pvc to keep,
      # so that the pvc is not deleted across reinstalls.
      resourcePolicy: ""

  resources: {}

# Opentelemetry-Collector
##########################################################################
otel:
  create: false
  image:
    repository: otel/opentelemetry-collector
    tag: 0.119.0
  service:
    type: ClusterIP
    port: 4317
    target_port: 4317
  fullnameOverride: "lomas-otel"
  config:
    processors:
      batch:
        timeout: 5s
    exporters:
      prometheus:
        port: 8889
    extension:
      health_check_port: 13133
      pprof_port: 1777
      zpages_port: 55679
  resources: {}

# Prometheus
##########################################################################
prometheus:
  create: false
  image:
    repository: prom/prometheus
    tag: v3.4.0
  service:
    port: 9091
    target_port: 9091
    type: ClusterIP
  fullnameOverride: "lomas-prometheus"
  config:
    scrape_interval: 5s
    evaluation_interval: 5s
  podSecurityContext: {}
    # fsGroup: 10001 # Match container group rights for volume access
  pvc:
    storageClassName: "" # is not set if empty string
    sizeLimit: 1Gi
    sizeRequest: 1Gi
    persistence:
      # If set to "keep", sets the helm/resource-policy of the pvc to keep,
      # so that the pvc is not deleted across reinstalls.
      resourcePolicy: ""
  resources: {}

# Tempo
##########################################################################
tempo:
  create: false
  image:
    repository: grafana/tempo
    tag: main-2765828
  service:
    type: ClusterIP
    port: 3000
    target_port: 3000

  fullnameOverride: "lomas-tempo"

  config:
    stream_over_http_enabled: true
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: "0.0.0.0:4317"
    storage:
      trace:
        backend: local
        # traces will be stored in <basepath>/traces and wal will be in <basepath>/wal
        basepath: /var/tempo/
  podSecurityContext: {}
    # fsGroup: 10001 # Match container group rights for volume access
  pvc:
    storageClassName: "" # is not set if empty string
    sizeLimit: 1Gi
    sizeRequest: 1Gi
    persistence:
      # If set to "keep", sets the helm/resource-policy of the pvc to keep,
      # so that the pvc is not deleted across reinstalls.
      resourcePolicy: ""
  resources: {}

# Grafana
##########################################################################
grafana:
  create: false

  image:
    repository: grafana/grafana
    tag: 11.0.0

  service:
    type: ClusterIP
    port: 3000
    target_port: 3000

  fullnameOverride: "lomas-grafana-dashboard"

  apiVersion: 1
  datasources:
    prometheus:
      name: Prometheus
      type: prometheus
      uid: prometheus
      access: proxy
      orgId: 1
      basicAuth: false
      isDefault: false
      version: 1
      editable: true
      jsonData:
        httpMethod: GET
    tempo:
      name: Tempo
      type: tempo
      uid: tempo
      access: proxy
      orgId: 1
      basicAuth: false
      isDefault: true
      version: 1
      editable: true
      apiVersion: 1
      stream_over_http_enabled: false
    loki:
      name: Loki
      type: loki
      uid: loki
      access: proxy
      orgId: 1
      basicAuth: false
      isDefault: false
      version: 1
      editable: true
      jsonData:
        httpHeaderName1: "X-Scope-OrgID"
      secureJsonData:
        httpHeaderValue1: "tenant1"

  security:
    admin_user: "admin"
    admin_password: "admin"

  ingress:
    enabled: true
    className: "nginx"
    annotations:
      nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    hostname: "lomas-grafana-dashboard-lance.lab.sspcloud.fr"
  # Openshift route for Openshift clusters.
  route:
    enabled: false
    annotations: {}
    hostname: "lomas-grafana-dashboard-lance.lab.sspcloud.fr"
    tls:
      insecureEdgeTerminationPolicy: Redirect
      termination: edge
  replicaCount: 1
  resources: {}

##########################################################################
# Lomas Chart Dependecies
##########################################################################

# RabbitMQ
##########################################################################
rabbitmq:
  create: true # Added by lomas
  heartbeat_client: 1800 # Added by lomas

  fullnameOverride: "rabbitmq"

  global:
    compatibility:
      openshift:
        adaptSecurityContext: auto
    security:
      allowInsecureImages: true

  image:
    registry: docker.io
    repository: bitnami/rabbitmq
    tag: 4.0.8-debian-12-r0
    digest: ""
    debug: false
    pullPolicy: IfNotPresent
    pullSecrets: []

  auth:
    username: guest
    password: guest
    securePassword: true
    updatePassword: false
    existingPasswordSecret: ""
    existingSecretPasswordKey: ""
    tls:
      enabled: false

  plugins: "rabbitmq_management rabbitmq_peer_discovery_k8s"

  extraConfiguration: |-
      heartbeat = 1800

  clustering:
    enabled: false

  containerPorts:
    amqp: 5672
    amqpTls: 5671
    dist: 25672
    manager: 15672
    epmd: 4369
    metrics: 9419

  podSecurityContext:
    enabled: true
    fsGroupChangePolicy: Always
    sysctls: []
    supplementalGroups: []
    fsGroup: 1001

  containerSecurityContext:
    enabled: true
    seLinuxOptions: {}
    runAsUser: 1001
    runAsGroup: 1001
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop: ["ALL"]
    seccompProfile:
      type: "RuntimeDefault"

  resourcesPreset: "small"
  resources: {}

  pdb:
    create: false
  serviceAccount:
    create: false
  rbac:
    create: false
  persistence:
    enabled: false
  persistentVolumeClaimRetentionPolicy:
    enabled: false

  service:
    type: ClusterIP
    portEnabled: true
    distPortEnabled: true
    managerPortEnabled: true
    epmdPortEnabled: true
    ports:
      amqp: 5672
      amqpTls: 5671
      dist: 25672
      manager: 15672
      metrics: 9419
      epmd: 4369
    portNames:
      amqp: "amqp"
      amqpTls: "amqp-tls"
      dist: "dist"
      manager: "http-stats"
      metrics: "metrics"
      epmd: "epmd"

  ingress:
    enabled: false

  # Openshift route for Openshift clusters, not part of bitnami chart.
  route:
    enabled: false
    labels: {}
    annotations: {}
    hostname: "rabbitmq-lance.lab.sspcloud.fr"
    tls:
      insecureEdgeTerminationPolicy: None
      termination: edge

  networkPolicy:
    enabled: false

  metrics:
    enabled: false
    plugins: "rabbitmq_prometheus"
    podAnnotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "{{ .Values.service.ports.metrics }}"


# Dex
##########################################################################
dex:
  create: true
  fullnameOverride: "lomas-dex"
  ingress:
    enabled: true
    hosts:
      - host: auth-lomaslm.lab.sspcloud.fr
        paths:
          - path: /
            pathType: Prefix
  rbac:
    create: false
  serviceAccount:
    create: false
  grpc:
    enabled: true
  service:
    ports:
      http:
        port: 4445
      grpc:
        port: 4446
  config:
    storage:
      type: "memory"
    enablePasswordDB: true
    issuer: https://auth-lomaslm.lab.sspcloud.fr
    oauth2:
      passwordConnector: local
    staticClients:
    - id: lomas_api
      name: lomas_api
      public: false
      secret: lomas_api
    - id: lomas_client
      name: lomas_client
      public: true
      redirectURIs:
      - /device/callback
    - id: lomas_dashboard
      name: lomas_dashboard
      public: false
      redirectURIs:
      - https://lomas-dashboard-lance.lab.sspcloud.fr/admin/oauth2callback
      secret: lomas_dashboard

    # staticPasswords:
    # - email: "lomas_admin@example.com"
    #   username: "lomas_admin"
    #   preferredUsername: "lomas_admin"
    #   userID: "lomas_admin"
    #   hash: "$2y$10$sdO1lq0.L9WZKlJrlxS7UuxrtdnvNTFAb573R0RZh9R.bPbH/lF4K"

