apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "lomas.server.fullname" . }}
  labels:
    {{- include "lomas.server.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.server.pvc.persistence.resourcePolicy }}
    "helm.sh/resource-policy": {{ .Values.server.pvc.persistence.resourcePolicy }}
    {{- end }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.server.pvc.storageClassName }}
  storageClassName: {{ .Values.server.pvc.storageClassName | quote }}
  {{- end }}
  resources:
    limits:
      storage: {{ .Values.server.pvc.sizeLimit | quote}}
    requests:
      storage: {{ .Values.server.pvc.sizeRequest | quote }}