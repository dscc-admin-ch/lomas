{{- if not .Values.oauth2-proxy.config.cookieSecretExistingSecretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "lomas.oauth2-proxy.cookieSecretName" . }}
  labels:
    {{ include "lomas.labels" . | nindent 4}}
type: Opaque
data:
  {{ include "lomas.oauth2-proxy.cookieSecretKey" . }}: {{ required ".Values.oauth2-proxy.config.cookieSecret" .Values.oauth2-proxy.config.cookieSecret | b64enc }}
{{- end }}
---
{{- if not .Values.oauth2-proxy.config.clientSecretExistingSecretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "lomas.oauth2-proxy.clientSecretName" . }}
  labels:
    {{ include "lomas.labels" . | nindent 4}}
type: Opaque
data:
  {{ include "lomas.oauth2-proxy.clientSecretKey" . }}: {{ required ".Values.oauth2-proxy.config.clientSecret" .Values.oauth2-proxy.config.clientSecret | b64enc }}
{{- end }}
